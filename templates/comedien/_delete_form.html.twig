<form method="post" action="{{ path('app_comedien_delete', {'id_comedien': comedien.idComedien}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comedien.idComedien) }}">
    <button class="btn">Delete</button>
</form>
