<form method="post" action="{{ path('app_contrat_da_delete', {'id_contrat_da': contrat_da.id_contrat_da}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contrat_da.id_contrat_da) }}">
    <button class="btn">Delete</button>
</form>
